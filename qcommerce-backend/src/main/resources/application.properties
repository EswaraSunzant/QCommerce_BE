spring.application.name=qcommerce-backend
#spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
spring.security.user.name=admin
spring.security.user.password=admin123

spring.datasource.url=jdbc:postgresql://localhost:5432/QCommerce
spring.datasource.username=quser
spring.datasource.password=qcommerce@123
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.springframework.security=DEBUG

# ===================================================================
# Corrected JWT Configuration
# ===================================================================
# IMPORTANT: Ensure this secret key is strong and securely managed.
# It should be the same value as your previous 'jwt.secret-key' if that was your intended secret.
jwt.secret=zVTcy5/pa/FisoRkSHSzvFNDMsC+9zq+RwZACTT5X5k=

# Token validity in milliseconds
# Your previous 'jwt.expiration' was 86400000 (1 day)
jwt.access-token.expiration-ms=86400000
# Your previous 'jwt.refresh-expiration' was 604800000 (7 days)
jwt.refresh-token.expiration-ms=604800000
jwt.issuer=qcommerce-app
